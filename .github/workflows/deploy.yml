name: Deploy NextJS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Copy files
        run: |
          rsync -av --delete ./ /var/www/nextjs-app/

      - name: Build & Start Container
        run: |
          cd /var/www/nextjs-app
          docker compose down || true
          docker compose build
          docker compose up -d
